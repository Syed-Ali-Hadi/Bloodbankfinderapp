
  # Blood Bank Finder App

  This is a code bundle for Blood Bank Finder App. The original project is available at
  https://talash-e-blood-app.netlify.app/

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  
